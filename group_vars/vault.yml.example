---
# Ansible Vault - Encrypted Secrets Template
# 
# INSTRUCTIONS:
# 1. Copy this file to vault.yml:
#    cp vault.yml.example vault.yml
#
# 2. Edit vault.yml with your real passwords
#
# 3. Encrypt the file:
#    ansible-vault encrypt vault.yml
#
# 4. NEVER commit vault.yml to git (it's in .gitignore)

# Administrative passwords (global)
vault_postgres_superuser_password: "CHANGE-ME-strong-password-here"
vault_postgres_admin_password: "CHANGE-ME-admin-password-here"
