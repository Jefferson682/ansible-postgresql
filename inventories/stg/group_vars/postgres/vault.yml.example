---
# Ansible Vault - Encrypted Secrets Template for STAGING environment
# 
# INSTRUCTIONS:
# 1. Copy this file to vault.yml:
#    cp vault.yml.example vault.yml
#
# 2. Edit vault.yml with your real passwords
#
# 3. Encrypt the file:
#    ansible-vault encrypt vault.yml
#
# 4. NEVER commit vault.yml to git (it's in .gitignore)

# STAGING environment passwords
vault_postgres_app_password: "CHANGE-ME-staging-app-password"
vault_postgres_app_password_readonly: "CHANGE-ME-staging-readonly-password"
